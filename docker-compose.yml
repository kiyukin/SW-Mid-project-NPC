version: "3.8"

services:
  npc-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: npc-backend
    ports:
      - "8089:8089"
    environment:
      - NPC_HOST=0.0.0.0
      - NPC_PORT=8089
    restart: unless-stopped
